# Project instructions

## GitHub

- Use the console utility `gh` for GitHub operations, including pull requests, issues, reviews, and workflow checks.
- Do not use the browser or GitHub connectors instead of `gh`. If `gh` is unavailable or authentication is missing, report the blocker and ask the user to resolve it.
