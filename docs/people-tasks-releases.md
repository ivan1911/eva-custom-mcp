# Люди, «мои задачи» и релизы

Статус: в разработке, ветка `feature/people-releases`. Поиск людей, задачи с `EVA_USER_LOGIN` и чтение релизов проверены на живом EvaTeam; операции записи ещё не проверены live.

## Зачем

До этого изменения ассистент не мог выполнить частые просьбы:

- «Назначь задачу на Петрова». `task_assign` требовал ссылку `CmfPerson:<uuid>`, а найти её было нечем.
- «Что у меня в работе?», «Где ждут моего ответа?». Не было способа узнать текущего пользователя и отфильтровать его задачи.
- «Что входит в релиз REL-92 и сколько осталось?», «Добавь задачу в релиз». Релизы не поддерживались совсем.

## Новые инструменты

### Люди

| Инструмент | Что делает | Вызов API |
| --- | --- | --- |
| `person_search` | Поиск по части имени или логина, без системных пользователей | `CmfPerson.list`, фильтр `name`/`login` `ILIKE` и `system == false` |
| `person_get` | Один пользователь по логину, уникальной части имени или ссылке | `CmfPerson.get` по `login` или по ссылке |
| `whoami` | Пользователь из `EVA_USER_LOGIN`; принадлежность токена не проверяется | `CmfPerson.get` по настроенному логину |

Как определяется человек (везде, где передаётся `person`, `responsible` или `owner`):

1. Ссылка `CmfPerson:<uuid>` используется как есть.
2. Иначе ищется точное совпадение логина.
3. Иначе поиск по части имени или логина: один результат — берём его; несколько — ошибка со списком кандидатов, изменение не выполняется; ни одного — ошибка «Person not found».

### «Мои задачи»

`my_tasks` возвращает задачи человека (по умолчанию — текущего пользователя):

- `role`: `responsible` — исполнитель (по умолчанию), `owner` — постановщик (`cmf_owner`), `waiting` — «Ждём ответа» (`waiting_for`);
- `statusType`: `OPEN`, `IN_PROGRESS`, `IN_REVIEW` или `CLOSED`; по умолчанию все, кроме `CLOSED`;
- `projectCode`, `limit`, `fields`.

Ответ содержит не более `limit` задач и `hasMore`: для проверки наличия продолжения запрашивается одна дополнительная задача.

Вызов: `CmfTask.list` с фильтром вида `[['responsible', '==', personId], ['cache_status_type', '!=', 'CLOSED']]`, сортировка по `-cmf_modified_at`.

### Релизы

Релизы (`REL-…`) и спринты (`SPR-…`) в EvaTeam — объекты одной модели `CmfList`. Задача связана с релизом через поле `fix_versions`, со спринтом — через `lists`.

| Инструмент | Что делает | Вызов API |
| --- | --- | --- |
| `release_list` | Релизы по проекту и названию; с `codePrefix: "SPR-"` — спринты | `CmfList.list`, фильтр `code ILIKE 'REL-%'` |
| `release_tasks` | Задачи релиза и точное количество по типам статусов | `CmfList.get` по коду, `CmfTask.list` с `['fix_versions', 'IN', [id]]` и `include_archived`, `CmfTask.count` на каждый тип статуса |
| `task_add_release` | Добавляет релиз в задачу, уже назначенные релизы сохраняются | `CmfTask.fix_versions.append` с `args: [taskId, releaseId]` |

## Изменения в существующих инструментах

- `task_create`, `task_update`: исполнитель (`responsible`) принимается как логин или имя; новый параметр `owner` — постановщик (`cmf_owner`). Люди передаются в API как `{ id }`.
- `task_create`: новый параметр `sprintCode` — кладёт задачу в спринт через `lists: ['SPR-…']`.
- `task_assign`: параметр `person` принимает логин, имя или ссылку; старый `personRef` поддерживается как deprecated-алиас. Если переданы оба параметра, их значения должны совпадать. Новый параметр `waitingFor` одновременно ставит «Ждём ответа» на того же человека; `status` меняет статус тем же вызовом.
- Генерация JSON Schema: поддержаны `enum` и целочисленные параметры.

**Совместимость:** существующие вызовы `task_assign` с `personRef` продолжают работать; в новых вызовах используйте `person`.

## Новая настройка

`EVA_USER_LOGIN` — логин пользователя для `whoami` и `my_tasks` без явного `person`. Остальные инструменты и `my_tasks` с `person` работают без этой настройки. Логин не обязательно принадлежит владельцу токена; права доступа по-прежнему определяются токеном.

## Откуда взяты вызовы

| Что | Источник |
| --- | --- |
| `CmfPerson.list` с фильтрами `user_local`, `system` | Пример KB-000060 |
| Поиск пользователя по `login` через `CmfPerson.get` | Примеры KB-000239, KB-000258 |
| Постановщик — поле `cmf_owner` | Пример KB-000239 |
| `responsible` и `waiting_for` как `{ id }`, смена статуса кодом `STC-…` в одном `CmfTask.update` | Пример KB-000258 |
| Значения `cache_status_type`: `OPEN`, `IN_PROGRESS`, `IN_REVIEW`, `CLOSED` | Примеры KB-000185, KB-000187 |
| Задача в спринте через `lists` | Пример KB-000074 |
| Задачи релиза через `fix_versions IN [id]` с `include_archived` | Пример KB-000269 |
| `CmfTask.fix_versions.append(taskId, releaseId)` | Спецификация OpenAPI 1.9.22, пример KB-000353 |
| `CmfList` для спринтов и релизов | Спецификация OpenAPI 1.9.22 (`CmfList.create` с примером «Спринт 1»), коды `REL-` в примерах |
| `CmfPerson.public_get_current_user` | На живом `/api/` возвращает `-32601 Method not found`; вызов удалён |

## Что проверить на живом EvaTeam

- [x] `CmfPerson.public_get_current_user` через `/api/` не поддерживается. `whoami` работает через `EVA_USER_LOGIN`; без логина возвращает ошибку настройки без RPC.
- [ ] `person_search`: поддерживает ли `CmfPerson.list` фильтр `ILIKE` по `name` и `login` в составе `OR`.
- [ ] `my_tasks`: работают ли фильтры `['responsible', '==', id]`, `['cmf_owner', '==', id]`, `['waiting_for', '==', id]`.
- [ ] `my_tasks`: возвращаются ли поля `status.name`, `responsible.name`, `parent.name` в формате `fields`.
- [ ] `release_list`: всегда ли коды релизов начинаются с `REL-` и есть ли у `CmfList` поле, которое отличает релиз от спринта (тогда префикс можно заменить на него).
- [ ] `release_list` с `projectCode`: связан ли релиз с проектом через `parent_id`.
- [ ] `task_create` с `sprintCode`: попадает ли задача в спринт.
- [ ] `task_assign` с `waitingFor`: ставится ли «Ждём ответа».

## Как проверить

1. Собрать ветку: `npm install && npm run build`.
2. Подключить локальную сборку к клиенту (раздел «Разработка» в README) с `EVA_BASE_URL` и `EVA_API_TOKEN`.
3. Попросить ассистента: «кто я в EvaTeam», «найди Петрова», «что у меня в работе», «где ждут моего ответа», «какие релизы в проекте PRJ», «что осталось закрыть в REL-…», «назначь DEV-… на Петрова и поставь ждём ответа».
4. Отметить пункты чек-листа выше; то, что не сработало, поправить до мерджа.

## Не вошло

- Создание релизов: `CmfList.create` принимает только `name` и `parent`, как пометить объект именно релизом, неизвестно.
- Удаление релиза из задачи: метода вроде `fix_versions.remove` в документации нет.
- Версия в `src/server.ts` (0.1.3) не совпадает с `package.json` (0.1.4) — отдельная правка.
